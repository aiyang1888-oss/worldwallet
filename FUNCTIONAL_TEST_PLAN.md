# 🧪 WorldWallet 功能验证测试计划

**开始时间**: 2026-04-08 09:16 UTC+7  
**测试范围**: 完整功能 + 安全修复验证  
**优先级**: 必须通过  

---

## 📋 测试清单

### 🔴 P0 - 核心功能验证

#### 1. PIN 与钱包解锁
- [ ] **创建新钱包**: 输入 PIN → 生成 12 字助记词
  - 验证: 助记词显示正确
  - 验证: localStorage 中 ww_wallet 和 ww_pin_hash 存在
  - 验证: IDB 迁移标记存在 (ww_idb_migrated_v1)

- [ ] **解锁钱包**: 输入正确 PIN → 钱包解锁
  - 验证: REAL_WALLET 加载成功
  - 验证: 三个地址正确显示 (ETH, TRX, BTC)
  - 验证: Console 无错误

- [ ] **错误 PIN 拒绝**: 输入错误 PIN → 显示错误提示
  - 验证: 钱包不解锁
  - 验证: 显示明确的错误消息

#### 2. 万语地址一致性 (需求 8)
- [ ] **初始地址记录**: 钱包解锁后
  ```
  初始 TRX 地址: ____________________
  ```
  - 在 Console 执行: `console.log('[WanYuAddr]', REAL_WALLET?.trx?.address)`

- [ ] **5 次刷新测试**:
  - 刷新 1: TRX 地址: __________________ → ✅ / ❌
  - 刷新 2: TRX 地址: __________________ → ✅ / ❌
  - 刷新 3: TRX 地址: __________________ → ✅ / ❌
  - 刷新 4: TRX 地址: __________________ → ✅ / ❌
  - 刷新 5: TRX 地址: __________________ → ✅ / ❌
  - 结论: ✅ 完全一致 / ❌ 有差异

- [ ] **localStorage 快照** (第 1 次和第 5 次刷新后对比):
  ```
  刷新 1 后:
    ww_wallet 前 100 字: ...
    ww_pin_hash: ...
    ww_pin_device_salt_v1: ...
    ww_idb_migrated_v1: ...
  
  刷新 5 后:
    ww_wallet 前 100 字: ...
    ww_pin_hash: ...
    ww_pin_device_salt_v1: ...
    ww_idb_migrated_v1: ...
  
  对比: ✅ 完全相同 / ❌ 有差异
  ```

- [ ] **浏览器完全重启**:
  - 完全关闭浏览器（所有标签页）
  - 等待 10 秒
  - 重新打开浏览器
  - 进入钱包页面
  - 刷新一次
  - 在 Console 执行: `console.log('[WanYuAddr] After restart:', REAL_WALLET?.trx?.address)`
  - 重启后 TRX 地址: __________________
  - 与初始地址比较: ✅ 完全相同 / ❌ 不同

#### 3. 会话私钥加密验证 (P0 安全修复)
- [ ] **会话密钥生成**: 钱包解锁后
  - 验证: `wwSealWalletSensitive()` 被调用
  - 验证: 会话加密密钥在内存中（Console 检查）
  - 验证: 私钥不在 REAL_WALLET 明文中
  ```javascript
  // Console 验证
  console.log('[Encrypt] REAL_WALLET.eth.privateKey:', REAL_WALLET?.eth?.privateKey);
  // 预期: undefined (已被加密)
  ```

- [ ] **转账使用临时解密**:
  - 进入转账页面
  - 验证: `wwWithWalletSensitive()` 正常工作
  - 验证: 转账可以执行
  - 验证: 转账后私钥仍被加密

#### 4. Base64 错误处理 (P0 安全修复)
- [ ] **无效 Base64 处理**:
  - 修改 localStorage 中的 ww_wallet，破坏 base64 数据
  - 刷新页面
  - 验证: Console 显示清晰的错误信息
  - 验证: 应用不崩溃
  - 验证: 显示"无效钱包数据"提示

#### 5. PIN 哈希升级 (P1 安全修复)
- [ ] **新用户 PIN 存储**:
  - 创建新钱包，设置 PIN
  - 验证: `ww_pin_device_salt_v1` 存在（新盐）
  - 验证: `ww_pin_hash` 是新格式哈希

- [ ] **旧用户 PIN 自动升级**:
  - （如有旧钱包）输入旧 PIN
  - 验证: 钱包解锁成功
  - 验证: `ww_pin_device_salt_v1` 自动创建
  - 验证: PIN 哈希更新为新格式

#### 6. IndexedDB 迁移 (P1 安全修复)
- [ ] **首次加载迁移**:
  - 打开页面
  - 验证: `ww_idb_migrated_v1 = true` 在 localStorage
  - 验证: IndexedDB 中有 ww_wallet 数据
  - 验证: 数据与 localStorage 一致

- [ ] **迁移后双写验证**:
  - 修改钱包设置（如备份状态）
  - 保存
  - 验证: localStorage 和 IndexedDB 同时更新
  - 验证: 数据一致

---

### 🟡 P1 - 完整流程测试

#### 7. 钱包创建流程
- [ ] **生成助记词**:
  - 点击"创建钱包"
  - 验证: 12 个英文单词生成
  - 验证: 每个单词间用空格分隔
  - 验证: 无重复单词

- [ ] **确认备份**:
  - 抄写或复制助记词
  - 点击"我已备份"
  - 验证: localStorage 更新 `backedUp: true`

- [ ] **派生地址**:
  - 验证: ETH 地址生成 (0x开头，40位十六进制)
  - 验证: TRX 地址生成 (T开头，34位)
  - 验证: BTC 地址生成 (1/3/bc1 开头)
  - 验证: 三个地址不同

#### 8. 转账流程 (需要测试网代币)
- [ ] **选择链和币种**:
  - 选择 TRX
  - 验证: 显示 TRX 余额（即使为 0）
  - 验证: 可输入金额

- [ ] **输入转账信息**:
  - 输入金额: 0.1
  - 输入接收地址: (另一个地址)
  - 验证: 金额验证通过
  - 验证: 地址格式验证通过

- [ ] **确认转账** (需要余额):
  - 点击"确认转账"
  - 输入 PIN
  - 验证: 交易签名
  - 验证: 交易发送
  - 验证: 显示交易哈希

- [ ] **错误情况处理**:
  - 输入无效金额 (负数/超大数)
  - 验证: 错误提示 (wwParsePositiveAmount)
  - 输入无效地址
  - 验证: 地址验证错误提示

#### 9. 备份导出
- [ ] **导出加密备份**:
  - 进入备份页
  - 点击"导出备份"
  - 输入 PIN
  - 验证: 生成加密的 JSON 或二维码
  - 验证: 文件可下载

- [ ] **备份验证**:
  - 验证: 备份文件包含加密的敏感数据
  - 验证: 助记词和私钥被加密
  - 验证: 地址信息包含在备份中

#### 10. 会话清理
- [ ] **页签隐藏清理**:
  - 打开钱包
  - 切换到其他标签页
  - 切换回
  - 验证: 可能需要重新输入 PIN（或保持解锁）
  - 验证: 私钥状态正确

- [ ] **5 分钟自动清理** (可选):
  - 解锁钱包
  - 等待 5 分钟（或按代码设置的时间）
  - 验证: 会话自动清除
  - 验证: 需要重新输入 PIN 才能访问私钥

---

### 🟢 P2 - 边界和错误处理

#### 11. 无效输入处理
- [ ] **PIN 输入**:
  - 输入超过 6 位数字
  - 验证: 拒绝或截断
  - 输入非数字
  - 验证: 拒绝输入

- [ ] **地址输入**:
  - 输入格式错误的地址
  - 验证: 显示验证错误
  - 输入正确格式但校验码错误
  - 验证: 显示"地址无效"

- [ ] **金额输入** (wwParsePositiveAmount):
  - 输入 0
  - 验证: 拒绝
  - 输入负数 (-10)
  - 验证: 拒绝
  - 输入 超大数 (1e20)
  - 验证: 拒绝或显示上限

#### 12. 网络错误恢复
- [ ] **离线状态**:
  - 断开网络
  - 进入钱包（本地数据应可用）
  - 验证: 可显示地址和余额
  - 验证: 无转账（需网络）

- [ ] **恢复网络**:
  - 恢复网络连接
  - 验证: 余额更新
  - 验证: 可以转账

#### 13. 浏览器兼容性
- [ ] **Chrome**: 
  - 打开页面 → ✅ / ❌
  - 解锁 → ✅ / ❌
  - 转账 → ✅ / ❌

- [ ] **Firefox**: (同上)
- [ ] **Safari**: (同上)

---

## 📊 测试结果总结

### 通过标准

| 项目 | 必须通过 | 可接受 |
|------|---------|--------|
| 万语地址一致性 | ✅ 100% | ❌ 99% |
| PIN 安全性 | ✅ 所有错误拒绝 | ❌ 部分通过 |
| 会话加密 | ✅ 私钥始终加密 | ❌ 部分路径失败 |
| Base64 错误处理 | ✅ 不崩溃 + 清晰错误 | ❌ 崩溃或模糊 |
| 转账流程 | ✅ 完整可用 | ❌ 部分功能失败 |

### 最终通过标准

✅ **所有 P0 项必须通过**  
✅ **P1 项 80% 以上通过**  
⚠️ **P2 项可逐步改进**  

---

## 📝 测试记录

测试日期: _________________  
测试人: _________________  
浏览器: _________________  
版本: _________________  

### P0 测试结果
```
1. PIN 与解锁: [ ] 通过
2. 万语地址一致性: [ ] 通过
3. 会话私钥加密: [ ] 通过
4. Base64 错误处理: [ ] 通过
5. PIN 哈希升级: [ ] 通过
6. IndexedDB 迁移: [ ] 通过
```

### P1 测试结果
```
7. 钱包创建流程: [ ] 通过
8. 转账流程: [ ] 通过
9. 备份导出: [ ] 通过
10. 会话清理: [ ] 通过
```

### P2 测试结果
```
11. 无效输入处理: [ ] 通过
12. 网络错误恢复: [ ] 通过
13. 浏览器兼容性: [ ] 通过
```

---

## 🎯 下一步

- [ ] 准备测试环境 (测试网代币、浏览器设置)
- [ ] 执行 P0 测试 (必须全部通过)
- [ ] 记录测试结果
- [ ] 修复发现的 bug
- [ ] 执行 P1 测试
- [ ] 执行 P2 测试
- [ ] 汇总测试报告

---

**测试计划完成**。准备开始执行。
