# BUG-005 - 服务器 wallet.css 文件未更新

**发现时间：** 2026-04-07 01:10  
**严重级别：** P0（阻塞所有测试）

## 问题

服务器上的 `wallet.css` 文件与 Git 仓库不一致：

- **本地文件（正确）：** `dist/wallet.css` line 928
  ```css
  .wallet-loading-overlay {
    position: absolute; inset: 0;
    background: rgba(0,0,0,0.72);
    display: none; flex-direction: column;  /* ✅ display: none */
    align-items: center; justify-content: center;
    z-index: 200;
  }
  ```

- **服务器文件（错误）：** `https://www.worldtoken.cc/wallet.css`
  ```css
  .wallet-loading-overlay {
    position: absolute; inset: 0;
    background: rgba(0,0,0,0.72);
    display: flex; flex-direction: column;  /* ❌ display: flex */
    align-items: center; justify-content: center;
    z-index: 200;
  }
  ```

## 影响

所有 overlay 元素在页面加载时就显示，用户无法使用钱包。

## 修复步骤

1. 检查本地 `dist/wallet.css` 是否正确（已确认 ✅）
2. 检查 `wallet-shell/wallet.css` 源文件（已确认 ✅）
3. **修复 CSS：** 确保所有 overlay 类基础样式都是 `display: none`
4. 重新部署 `dist/wallet.css` 到服务器
5. 清除 CDN 缓存
6. 验证修复

## 需要检查的 CSS 规则

所有 overlay 类的基础样式都应该是 `display: none`:

- `.wallet-loading-overlay`
- `.overlay`
- `.qr-overlay`
- `.pin-setup-overlay`

`.show` class 用于显示：`display: flex`

---

**状态：** 等待修复和部署
