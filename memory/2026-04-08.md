# 2026-04-08

## WorldWallet 深度功能检查

- `node scripts/ww-deep-check.mjs`：dom-bind / goTo / TAB_MAP / onclick 均 OK。
- 脚本核对：`index.html` 中所有 `goTo('page-…')` 目标均存在；JS 字面量 `goTo` 目标共 13 个，均在 HTML 的 20 个 `page-*` 内。
- 架构：`wallet.ui.js` 先于 `wallet.runtime.js` 加载，后者覆盖 `goTo`、`updateQRCode` 等 **109** 个同名顶层函数；易产生行为漂移。
- 已修复：`runtime` 的 `goTo` 在 `page-import` 补 `_wwInFirstRun`；`page-home` 补 `updateQRCode` 延迟刷新；`updateQRCode` 优先委托 `wwBuildReceiveQrPayload` / `wwGenerateQRCode`（含 BTC/金额）。
- Chrome 深度检查：`scripts/ww-chrome-deep-check.mjs`（Puppeteer `channel: chrome`）→ `DEEP_INSPECTION_CHROME_REPORT.md`；修复 `updateQRCode` 内 `chain`/`addr` 重复声明导致整文件未执行；删除 `wallet.runtime.js` 内覆盖 `renderKeyGrid` 的副本（恢复 ui 版 TEMP_WALLET 逻辑）。
