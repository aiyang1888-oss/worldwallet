- 落地中文 BIP39 地名助记词流水线：`npm run wordlist:zh` → generate-zh-cn-wordlist → verify-zh-wordlist → ww-build-wordlists；文档 `scripts/README-wordlists.md`；`npm run pcas:fetch` 更新区划 JSON。
- `wwPurgeLocalWalletStorage` / `wwClearAllUserCaches`：除 `ww_*` localStorage 外，清 sessionStorage、`wallet_*` 展示键、IndexedDB `WorldWalletKV`、Cache Storage、内存缓存与万语 ADDR、TEMP_WALLET。
