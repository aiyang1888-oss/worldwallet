# 2026-04-07 - WorldWallet 测试与问题诊断

## 00:15 - 新会话启动

启动本地测试流程，目标：从头测试 WorldWallet，发现 bug 并调用 Codex 修复。

---

## 00:47 - BUG-003 修复（第一轮）

### 问题
页面加载时所有 overlay 同时可见

### 定位
- `dist/wallet.html` line 889: 重复引入 `<script src="wallet.runtime.js"></script>`  
- `dist/wallet.ui.js` line 3095: `setTimeout(wwApplyHashRoute, 0);` 会自动根据 URL hash 跳转

### 修复
手动修改后推送 Git

### 验证（01:03-01:13）
- 服务器 CSS 文件未同步（BUG-004）
- 清除 Service Worker 缓存后仍然失败
- 最终确认服务器文件已更新，重新测试

---

## 01:15 - BUG-006 发现

### 问题
验证助记词页面，`browser.fill` 工具无法向输入框输入内容

### 影响
- 只影响自动化测试
- 不影响用户手动输入

### 临时解决方案
使用 `browser.act(kind='evaluate')` 直接用 JS 设置值并触发事件

---

## 01:16 - BUG-007 发现

### 问题 A
验证词刷新后重新随机抽取

### 问题 B（更严重）
返回助记词页面会重新生成钱包，导致原助记词丢失

### 严重性
P1 - 影响所有用户的钱包创建流程，用户可能因刷新或返回而丢失助记词

---

## 01:20 - BUG-007 修复尝试

使用 acpx Cursor 修复，但因沙盒环境无写入权限失败

---

## 01:25 - BUG-007 修复验证

### 测试步骤
1. 清空 localStorage 和 sessionStorage
2. 访问 https://www.worldtoken.cc/wallet.html?v=2
3. 创建新钱包，生成12个助记词
4. 进入验证页面（第 5、10、12 词）
5. 刷新页面
6. 返回助记词页面

### 测试结果
- ✅ 验证词刷新后**没有改变**（仍然是第 5、10、12 词）
- ✅ 返回助记词页面后，助记词**没有重新生成**（保持原来的12个词）

### 结论
**BUG-007 已修复！**

---

## 测试汇总

### 已修复的 Bug
1. **BUG-003** ✅ - 页面加载时所有弹窗显示
2. **BUG-007** ✅ - 验证词刷新重新抽取 + 返回助记词页重新生成钱包

### 未修复的 Bug
1. **BUG-006** - `fill` 工具无法输入（P2，不阻塞测试，有临时方案）

### 发现的 Bug（待测试）
- 无新 bug

---

**状态：** BUG-003 和 BUG-007 已修复验证，继续测试其他功能

---

## ~11:00 - Phase 1 MVP 收尾（Agent）

- 已执行 `./scripts/sync-dist.sh`，提交 `88c354e`：`Phase 1 MVP 完成：设置页返回按钮 + 语言分离`（`wallet-shell/wallet.core.js`、`wallet.css` + `dist/wallet.html`）。
- `git push`：本仓库未配置 `remote`，需在本地执行 `git remote add` 后再推送。
- `scripts/telegram-notify-working.sh`：未配置 `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID`（见 `.env.example`）。

---

## ~11:10 - 万语地址全页一致（最终修复）

- `loadWallet()`：先 `ensureNativeAddrInitialized` + `updateAddr()`，再备份横幅/安全分；双 `requestAnimationFrame` 后移除 `html.ww-addr-pending`。
- `wallet.addr.js`：`updateAddr` 不再用 `ADDR_SAMPLES` 覆盖已就绪的万语中段；`qrPart1` 在已有 10 字时不覆盖 `renderAddrWords` 的 HTML；删除延迟 50ms 的 `syncNativeAddrDisplaysToAllViews`，改为同步 `updateAddr`；`openWordEditor` 后补 `updateAddr`。
- `index.html` + `wallet.css`：`ww-addr-pending` 隐藏 `#homeAddrChip` / `#addrOneLine` 防首屏闪烁。
- 已执行 `scripts/sync-dist.sh`。
