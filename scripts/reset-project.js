#!/usr/bin/env node
/**
 * Expo 模板自带的 reset 脚本已移除。本项目以 app/ + dist/wallet.html 为主。
 * 如需清空 node_modules 请手动：rm -rf node_modules && npm install
 */
console.log(
  '[reset-project] 未执行任何操作。WorldWallet 使用 Expo Router（app/）与单页钱包（dist/wallet.html / public/wallet.html）。'
);
process.exit(0);
